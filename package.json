{
  "name": "webpack-jsdoc-closure-loader",
  "version": "1.0.0",
  "description": "Webpack loader that converts {module:path/to.namedExportType} and {module:path/to/DefaultExportType} style JSDoc types to Closure Compiler imported types ",
  "main": "index.js",
  "scripts": {
    "test": "eslint index.js test/ && mocha"
  },
  "author": "Andreas Hocevar",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "comment-parser": "^0.5.0",
    "eslint": "^4.17.0",
    "eslint-config-openlayers": "^9.0.0",
    "mocha": "^5.0.0"
  },
  "eslintConfig": {
    "extends": "openlayers",
    "env": {
      "es6": true
    }
  }
}
